<nav class="navbar navbar-expand-lg navbar-light bg-primary">
  <div class="container-fluid">
    <%= link_to root_path do %>
      <div class="navbar-brand nav-link" style="color: #FFF; border: #FFF solid 2px; padding: 5px; border-radius: 4px"><b><sup>The</sup>Blog</b></div>
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'Articles', root_path, class: "nav-link #{'active' if (current_page? root_path) }" %>
        </li>
        <li class="nav-item">
          <%= link_to 'New Article', new_post_path, class: "nav-link #{'active' if (current_page? new_post_path) }" %>
        </li>
      </ul>
      
      <ul class="navbar-nav justify-content-end">
        <% if user_signed_in? %>
          <div class="btn-group">
            <button class="btn btn-info dropdown-toggle" data-bs-toggle="dropdown" data-bs-display="static" aria-expanded="false">
              Account
            </button>
            <ul class="dropdown-menu dropdown-menu-end"  aria-labelledby="dropdownMenuLink">
              <li class="dropdown-item">
                <%= link_to 'My account', edit_user_registration_path, class: "dropdown-item" %>
              </li>
              <li><hr class="dropdown-divider"></li>
              <li class="dropdown-item">
                <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: "dropdown-item" %>
              </li>
            </ul>
          </div>
        <% else %>  
          <li class="nav-item">
            <%= link_to 'Log In', user_session_path, class: "nav-link #{'active' if (current_page? user_session_path) }" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Sign Up', new_user_registration_path, class: "nav-link #{'active' if (current_page? new_user_registration_path) }" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>